name: CI
on: push
jobs:
  deploy:
    runs-on: ubuntu-20.04
    steps:
      - name: Git checkout
        uses: actions/checkout@v2

      - name: Setup hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: "0.89.4"

      - name: Build
        # remove --minify tag if you do not need it
        # docs: https://gohugo.io/hugo-pipes/minification/
        run: hugo --minify

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          personal_token: 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
          external_repository: saihv.github.io
          publish_dir: ./public
          #   keep_files: true
          user_name: saihv
          user_email: saihemachandra.v@gmail.com
          publish_branch: master
          cname: saihv.com